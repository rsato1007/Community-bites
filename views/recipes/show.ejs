<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Recipes</title>
</head>
<body>
  <%- include('../partials/navbar') %>
  <h1>All Recipes</h1>
  <table>
  <thead>
    <tr>
      <th>Receipe Name</th>
      <th>Ingredients</th>
      <th>Steps</th>
      <th>Update <br> Recipe</th>
      <th>Delete <br> Recipe</th>
    </tr>
  </thead>
  <tbody>
    <% recipe.forEach(function(m) { %>
      <tr>
        <td><%= m.recipeName %></td>
        <td><%= m.ingredients %></td>
        <td><%= m.steps %></td>
        <td>
          <form action="/recipes/<%= m._id %>" method="GET">
            <button type="submit">Update</button>
          </form>
        </td>
        <td>
          <form action="/recipes/<%= m._id %>?_method=DELETE" method="POST">
            <button type="submit">Delete</button>
          </form>
        </td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  
</body>
</html>